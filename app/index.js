// index.js
import { registerRootComponent } from 'expo';

import App from './App'; // Import your main App component

// registerRootComponent tells Expo to start rendering your App component
// This is the standard entry point for Expo managed workflow apps.
registerRootComponent(App);
